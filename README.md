# 三省六部 Agent 系统

> 基于 Claude Code 自迭代机制的智能 Agent 架构，融合中国古代三省六部制

## 📋 项目概述

本系统将 Claude Code 的 Tool-Use 循环、验证驱动、自动重试等机制与中国古代三省六部制相结合，构建一个具有多层级迭代、动态规则注入、成本控制的智能 Agent 系统。

**核心特性：**
- 🏛️ **三省流程**：中书省（决策）→ 门下省（审议）→ 尚书省（协调）
- ⚙️ **六部执行**：吏、户、礼、兵、刑、工六部分工协作
- 🔄 **三层迭代**：封驳迭代、Sub Agent 自迭代、验收迭代
- 📊 **成本控制**：$5/任务、$100/天、$2000/月 预算上限
- 🛡️ **规则系统**：动态加载、优先级控制、运行时生效
- 🚨 **紧急模式**：检测紧急关键词，跳过审议直接执行

## 🏗️ 架构

```
┌─────────────────────────────────────────────┐
│              用户（皇帝）                    │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│          路由层（GLM-4.7）                  │
│  简单 → 直接 | 中等 → 单部门 | 复杂 → 三省  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│        共享上下文层（< 5KB 压缩历史）        │
└─────────────────────────────────────────────┘
                    ↓
    ┌───────────────┼───────────────┐
    ↓               ↓               ↓
┌────────┐                    ┌────────┐
│ 中书省 │◄───── 申诉 ───────│ 门下省 │
│ (决策) │                    │ (审议) │
│GLM-4.7 │                    │ GLM-5  │
└────────┘                    └────────┘
    └───────────────┬───────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│         尚书省（协调 + 资源锁 + 事件）       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│              六部（执行层）                  │
│  吏部(GLM-4.7)  户部(GLM-4.7)  礼部(GLM-4.7)│
│  兵部(GLM-5)    刑部(GLM-4.7)  工部(GLM-5)  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│         工部 Sub Agents（隔离执行）          │
│  Coder → Tester → Fixer（自迭代 ≤5次）      │
└─────────────────────────────────────────────┘
```

## 📁 目录结构

```
~/.openclaw/
├── agents/                  # Agent 系统核心
│   ├── types.ts            # 类型定义
│   ├── router.ts           # 路由层
│   ├── shared-context.ts   # 共享上下文
│   ├── rule-loader.ts      # 规则加载器
│   ├── orchestrator.ts     # 主协调器
│   ├── zhongshu/           # 中书省
│   ├── menxia/             # 门下省
│   ├── shangshu/           # 尚书省
│   ├── libu/               # 吏部
│   ├── hubu/               # 户部
│   ├── libu2/              # 礼部
│   ├── bingbu/             # 兵部
│   ├── xingbu/             # 刑部
│   └── gongbu/             # 工部
├── rules/                   # 规则系统
│   ├── rules.json          # 规则索引
│   ├── R001.yaml           # 非技术问题反馈
│   ├── R002.yaml           # 成本预算控制
│   ├── R003.yaml           # 紧急强制执行
│   └── R004.yaml           # 预览确认模式
└── workspace/               # 工作区（独立 git）
    └── memory/
        └── claude-code-architecture.md  # 架构设计文档
```

## 🚀 开发进度

| Phase | 任务 | 状态 | 预估 |
|-------|------|------|------|
| Phase 0 | 基础设施 | ✅ 完成 | 1-2天 |
| Phase 1 | 核心框架（三省） | ⏳ 待开始 | 2-3天 |
| Phase 2 | 六部实现 | ⏳ 待开始 | 2-3天 |
| Phase 3 | 迭代与安全 | ⏳ 待开始 | 1-2天 |
| Phase 4 | 成本与监控 | ⏳ 待开始 | 1-2天 |
| Phase 5 | 用户体验 | ⏳ 待开始 | 1-2天 |
| Phase 6 | 测试与优化 | ⏳ 待开始 | 1-2天 |

## 🎯 模型分配

| 组件 | 模型 | 原因 |
|------|------|------|
| 路由层 | GLM-4.7 | 快速分类，成本低 |
| 中书省 | GLM-4.7 | 任务拆分，中等复杂度 |
| 门下省 | GLM-5 | 审议决策，强推理 |
| 尚书省 | GLM-5 | 协调复杂，需强推理 |
| 吏/户/礼/刑部 | GLM-4.7 | 标准任务 |
| 兵/工部 | GLM-5 | 安全/工程需强推理 |

## 📊 成本预算

```yaml
perTask:   $5 USD    # 单任务上限
perDay:    $100 USD   # 每日上限
perMonth:  $2000 USD  # 每月上限

监控：
- 实时追踪
- 80% 警告
- 100% 暂停
```

## 🔧 规则系统

### R001: 非技术问题及时反馈
- 触发：权限/账号问题重试 ≥3 次
- 动作：立即停止，反馈用户

### R002: 成本预算控制
- 触发：接近或超过预算
- 动作：警告 → 暂停 → 询问用户

### R003: 紧急强制执行
- 触发：检测"紧急"/"生产"/"critical"等关键词
- 动作：跳过审议，直接执行，事后补充验收

### R004: 预览确认模式
- 触发：复杂任务、多文件修改
- 动作：显示执行计划，等待用户确认

## 📝 相关文档

- [架构设计文档](workspace/memory/claude-code-architecture.md)
- [Claude Code 机制分析](workspace/memory/claude-code-architecture.md#1-claude-code-核心架构)

## 📜 Git 提交历史

```
c6480ea - feat: Phase 0 - 基础设施完成 (2026-04-05)
```

## 🤝 贡献

这是个人项目，暂不接受外部贡献。

## 📄 许可

MIT

---

**构建时间**: 2026-04-05  
**当前版本**: v0.1.0-alpha  
**下一里程碑**: Phase 1 - 核心框架
